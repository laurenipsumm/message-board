<!DOCTYPE html>
<html>

<head>
  <title><%= title %></title>
  <link rel='stylesheet' href='/stylesheets/style.css' />
</head>

<body>
  <h1><%= title %></h1>
  <p>Welcome to <%= title %></p>
  <p>Message Board
    <ul>
      <% messages.forEach(function(message){ %>
      <li><%= `Message from ${message.user}: ${message.text}` %>
        <ul>
          <li>
            <%= message.added %>
          </li>
        </ul>
      </li>
      <% }); %>
    </ul>
    <a href="/new">Write a new message</a>
  </p>
</body>

</html>